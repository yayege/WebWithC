#include <stdio.h>
#define SIZE 100

int main(int argc, char*argv[]){ 
   char input[SIZE]=argv[1];
   char opt[SIZE]=argv[2]; 
   char search[SIZE];
   char replace[SIZE]; 
   
   

   int k=0; 
   for(int i=0;i<SIZE && i!='/' ;i++){ 
        input[i]=search[i]; 
   }
   search[i+1]='\0';     
   for(;i<SIZE && i!='/';i++){
         input[i]=replace[k];
         k++;
}
     i++; 
    input[i]='\0'; replace[k+1]='\0';
    
    if( strcmp(opt,"-F"){

		FILE *f;
		f=fopen(argv[3], "r+w");
  		while(!feof){ 
                fscanf("%s",)



	}              

